import Component from '@ember/component';
import layout from '../templates/components/modal-button';

export default Component.extend({
  layout,

  actions: {
    triggerModal() {
      return;
    }
  }
});
